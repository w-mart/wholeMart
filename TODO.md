# TODO

- [x] Identify failing JSP expression throwing EL 500 on /ai-chat.jsp
- [x] Fix Jakarta EL parsing issue: remove invalid `new Date(...)` inside `${...}`
- [ ] Rebuild/clear JSP cache if needed and retest the AI chat page


